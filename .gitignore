# Build output
dist/
.astro/

# Generated from tokens/twiceover.tokens.json at build (ADR 0004 / consultation 0005)
src/styles/tokens.css

# Dependencies
node_modules/

# Never commit secrets — the Cloudflare token comes from macOS Keychain at deploy time
.dev.vars
.env
.env.*
*.local

# Wrangler / OS
.wrangler/
.DS_Store

# Local worktrees under the old .claude/worktrees/ convention (ADR 0025 Amendment 3,
# stock-analyst-platform #123) — new worktrees live at ~/git/twiceover-web-wt/ (sibling
# dir, outside this tree entirely); this stays only to keep any grandfathered local
# worktree from being committed by accident.
.claude/worktrees/
