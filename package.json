{
  "name": "twiceover-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TwiceOver pre-launch compliance site — twiceover.io (ADR 0002, #39)",
  "scripts": {
    "prebuild": "node scripts/build-tokens.mjs",
    "build": "astro build",
    "predev": "node scripts/build-tokens.mjs",
    "dev": "astro dev",
    "check": "node ci/check-content.mjs",
    "gen:favicons": "node scripts/gen-favicons.mjs",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "^5.2.0",
    "@resvg/resvg-js": "^2.6.2",
    "wrangler": "^4.0.0"
  }
}
